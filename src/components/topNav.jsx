import React from 'react'
import styled from 'styled-components'
import { Button } from 'antd'

const styledNavWrapper = styled(Button)`
  background-color: red;

`

export default topNav = () => (
  <styledNavWrapper />
)
